version: '3.8'

services:
  mindmap-app:
    build: .
    container_name: mindmap-app
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - TZ=Asia/Saigon
